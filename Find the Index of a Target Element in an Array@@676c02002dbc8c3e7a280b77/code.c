#include<stdio.h>
void mian(){
    int n,t;
    scanf("%d",&n);
    int a[n];
    for(int i=0;i<n;i++){
        scanf("%d",&a[i]);
    }
    int target;
    scanf("%d",&target);
    for(int i=0;i<n;i++){
        if(target==a[i]);
    }
    printf("%d",i);
}